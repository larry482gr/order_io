<p id="notice"><%= notice %></p>

<h1>Tables</h1>

<table class="table table-hover table-responsive">
  <thead>
    <tr>
      <th>Label</th>
      <th>Language</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tables.each do |table| %>
      <tr>
        <td><%= table.label %></td>
        <td><%= table.language %></td>
        <td><%= link_to 'Show', table, class: 'btn btn-primary btn-xs' %></td>
        <td><%= link_to 'Edit', edit_table_path(table), class: 'btn btn-warning btn-xs' %></td>
        <td><%= link_to 'Destroy', table, class: 'btn btn-danger btn-xs', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Table', new_table_path %>
