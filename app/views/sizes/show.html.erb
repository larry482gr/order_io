<p id="notice"><%= notice %></p>

<p>
  <strong>Label:</strong>
  <%= @size.label %>
</p>

<%= link_to 'Edit', edit_size_path(@size), class: 'btn btn-warning btn-xs' %> |
<%= link_to 'Back', sizes_path, class: 'btn btn-default btn-xs' %>
