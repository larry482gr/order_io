<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @waiter.name %>
</p>

<%= link_to 'Edit', edit_waiter_path(@waiter), class: 'btn btn-warning btn-xs' %> |
<%= link_to 'Back', waiters_path, class: 'btn btn-default btn-xs' %>
